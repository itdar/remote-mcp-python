[project]
name = "ppyy"
version = "0.1.0"
description = "Sample codes for Python MCP server"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.12.4",
]
